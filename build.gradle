plugins {
    id 'java'
    id 'application'
}

mainClassName = 'example.ExampleApplication'

dependencies {
    compile group: 'io.dropwizard', name: 'dropwizard-core', version: '2.0.15'
}

repositories {
    jcenter()
}
